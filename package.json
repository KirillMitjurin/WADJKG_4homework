{
    "name": "project-root",
    "version": "1.0.0",
    "private": true,
    "scripts": {
      "start": "npm run start:backend & npm run serve:frontend",
      "serve:frontend": "npm --prefix frontend run serve",
      "build:frontend": "npm --prefix frontend run build",
      "start:backend": "npm --prefix backend run start",
      "install": "npm install && npm --prefix frontend install && npm --prefix backend install"
    }
  }
  